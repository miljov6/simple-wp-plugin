# simple-wp-plugin
Simple WP Plugin for showing already generated top bar message

If textarea field is empty, show on all pages, else, only on pages which are containing strings from input inside the url slugs
